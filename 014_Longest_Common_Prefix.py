class Solution:
    def longestCommonPrefix(self, strs: list[str]) -> str:
        def find_common(s1: str, s2: str) -> str:
            for char in enumerate(s1)

        ref = strs[0]



print(Solution().longestCommonPrefix(["flower", "flow", "flash"]))